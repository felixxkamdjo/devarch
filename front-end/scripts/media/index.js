export {
    uploadFile,
    getFileMetadata
} from './upload.js';

import {
    uploadFile,
    getFileMetadata
} from './upload.js';

export const mediaModule = {
    uploadFile,
    getFileMetadata
};
